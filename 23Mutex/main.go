package main

import "fmt"

// mutex are data structure provide by sync package
// they can help us  a lock on different sections of data so that only goroutine ca access it at a time.
func main() {
	fmt.Println("mutex")
}
